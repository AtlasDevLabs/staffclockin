Config = {
    AcePerm = 'atlas.cmds',
    Keybind = 'F9', -- Default you can change it or leave it blank to just use the command
    Notify = 0, --- 0 (chatmessage) --- 1 (okokNotify) --- 2 (codem) --- 3 (mythic)
    NotifyDuration = 8000, -- 8000 (8 seconds) by default change to whatever you like
    ClockinMessage = 'You have Clocked In',
    ClockoutMessage = 'You have Clocked Out',
    Webhook = '',
}
